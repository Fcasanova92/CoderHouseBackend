# Mi App Node.js con Docker

https://hub.docker.com/r/fcasa/coderbackend

## Variables de entorno necesarias

Crea un archivo `.env` basado en `.env.example` y configura tus valores:
    PORT=3000 o cualquier otro
    MONGODB_URI=mongodb://mi-mongo:27017/app